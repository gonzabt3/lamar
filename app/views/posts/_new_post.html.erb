<nav class="box">
  <%= form_with(model: @post, url: posts_path) do |form| %>
  <div class="field">
    <label class="label">Nueva Publicación</label>
    <div class="control">
      <%= form.text_area :body, class:"textarea", placeholder:"Hay terea?" %>
    </div>
  </div>
  <div class="buttons">
    <%= form.submit "Publicar", class: "button is-primary is-fullwidth" %>
  </div>
  <% end %>
</nav>